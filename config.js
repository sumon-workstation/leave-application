/* Deployment settings. Edit this file, commit, done. */
window.CONFIG = {

  /* Paste the Apps Script Web App URL here after you deploy Code.gs.
     Leave it empty and the form still works — it just falls back to PDF only. */
  endpoint: "",

  /* Weekend days, 0 = Sunday. Bangladesh default is Friday + Saturday. */
  weekend: [5, 6],

  /* Shown on the confirmation screen so people know who to chase. */
  hrContact: "connect@livecorps.com"
};
